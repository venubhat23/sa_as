<% page_title "Notifications" %>

<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h2 class="mb-1">Notification Management</h2>
    <p class="text-muted mb-0">Manage your notifications here</p>
  </div>
  <div>
    <%= link_to new_admin_notification_path, class: "btn btn-primary" do %>
      <i class="bi bi-plus-lg me-1"></i> Add New Notification
    <% end %>
  </div>
</div>

<!-- Table Card -->
<div class="card border-0 shadow-sm">
  <div class="card-header bg-transparent border-0">
    <h5 class="mb-0">Notification List</h5>
  </div>
  <div class="card-body p-0">
    <% if @notifications.any? %>
      <div class="table-responsive">
        <table class="table table-hover mb-0">
          <thead class="table-light">
            <tr>
              <th class="border-0 fw-semibold">Title</th>
              <th class="border-0 fw-semibold">Type</th>
              <th class="border-0 fw-semibold">Read</th>
              <th class="border-0 fw-semibold">Sent</th>
              <th class="border-0 fw-semibold">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @notifications.each do |f| %>
              <tr>
                <td class="align-middle"><%= f.title %></td>
                <td class="align-middle"><%= f.notification_type %></td>
                <td class="align-middle"><%= f.is_read ? 'Yes' : 'No' %></td>
                <td class="align-middle"><%= f.sent_at&.strftime('%d %b %Y') %></td>
                <td class="align-middle">
                  <div class="dropdown">
                    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                      <i class="bi bi-three-dots"></i>
                    </button>
                    <ul class="dropdown-menu">
                      <li><%= link_to admin_notification_path(f), class: "dropdown-item" do %><i class="bi bi-eye me-2"></i>View<% end %></li>
                      <li><%= link_to edit_admin_notification_path(f), class: "dropdown-item" do %><i class="bi bi-pencil me-2"></i>Edit<% end %></li>
                      <li><hr class="dropdown-divider"></li>
                      <li><%= link_to admin_notification_path(f), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "dropdown-item text-danger" do %><i class="bi bi-trash me-2"></i>Delete<% end %></li>
                    </ul>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <div class="card-footer bg-transparent border-0">
        <%= paginate @notifications if @notifications.respond_to?(:current_page) %>
      </div>
    <% else %>
      <div class="text-center py-5">
        <i class="bi bi-bell-fill fs-1 text-muted mb-3 d-block"></i>
        <h5 class="text-muted mb-3">No Notifications Found</h5>
        <p class="text-muted mb-4">Get started by adding your first notification.</p>
        <%= link_to "Add Notification", new_admin_notification_path, class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>
